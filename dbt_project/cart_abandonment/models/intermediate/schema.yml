version: 2

models:
  - name: int_sessions
    description: "GA4 events grouped into sessions using 30-minute inactivity window"
    columns:
      - name: session_id
        description: "Unique session identifier (user_id + session number)"
        tests:
          - not_null
      - name: user_id
        tests:
          - not_null
